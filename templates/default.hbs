<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://fonts.googleapis.com/css2?family=Spartan:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Spartan', sans-serif;
      margin: 0;
      padding: 0;
      color: #444;
    }
    p {
      margin: 0;
    }
    .project-info {
      padding: 20px;
      margin: 20px;
      background-color: #1ccad8;
      color: #fff;
    }
    .project-info p {
      font-size: 18px;
      margin-bottom: 10px;
    }
    .project-info p span {
      font-weight: 700;
    }
    .table {
      padding: 20px;
    }
    .table__row {
      display: flex;
      justify-content: space-between;
    }
    .table__row:nth-child(2n+2) {
      background-color: #f1f1f1;
    }
    .table__row--head {
      font-weight: 700;
    }
    .table__cell {
      padding: 20px 10px;
    }
    .table__cell:nth-child(1) {
      flex: 1;
    }
    .table__cell:nth-child(2) {
      width: 200px;
    }
    .table__cell:nth-child(3) {
      width: 100px;
    }
    footer {
      margin: 20px;
      padding: 20px 0 0;
      border-top: 1px solid #f1f1f1;
    }
    .date {
      font-size: 18px;
      padding-bottom: 20px;
      margin-bottom: 20px;
      border-bottom: 1px solid #f1f1f1;
    }
    .price {
      font-size: 24px;
      font-weight: 700;
    }
  </style>
</head>
<body>
  <div class="project-info">
    <p>Project name: <span>{{ projectInfo.name }}</span></p>
    <p>Client name: <span>{{ projectInfo.client }}</span></p>
    <p>Start date: <span>{{ projectInfo.start }}</span></p>
  </div>
  <div class="table">
    <div class="table__row table__row--head">
      <div class="table__cell">Task</div>
      <div class="table__cell">Days</div>
      <div class="table__cell">Cost</div>
    </div>
    {{#each tasks}}
    <div class="table__row">
      <div class="table__cell">{{ this.name }}</div>
      <div class="table__cell">{{ this.days }}</div>
      <div class="table__cell">{{ this.cost }}</div>
    </div>
    {{/each}}
  </div>
  <footer>
    <p class="date">Delivery date: {{ deliveryDate }}</p>
    <p class="price">Total price ${{ totalPrice }}</p>
  </footer>
</body>
</html>